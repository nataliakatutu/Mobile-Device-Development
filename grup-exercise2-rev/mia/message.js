// demonstrating default export.

const message = () => {
    const name = "Tokyo";
    const members = "nat, sher, mia, enji, hen, cher";
    return name + " members is " +  members;
    };
    
    export default message;